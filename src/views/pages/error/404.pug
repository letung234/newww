doctype html
html
  head
    title Trang không tìm thấy
    link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css')
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale=1')
  body(class='bg-gray-100 flex items-center justify-center h-screen')
    .container(class='bg-white p-8 rounded-lg shadow-lg max-w-md text-center')
      h1(class='text-6xl font-bold text-red-600') 404
      h2(class='text-2xl font-semibold text-gray-800 mt-4') Trang không tìm thấy
      p(class='mt-2 text-gray-600') Có vẻ như trang bạn đang tìm không tồn tại.
      a(href='/' class='mt-4 inline-block text-blue-500 hover:text-blue-700 font-semibold') Quay lại trang chủ
